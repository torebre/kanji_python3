class RelativePositionData:

    def __init__(self, row_diff: float, col_diff: float, angle: float, id_line_to: int):
        self.row_diff = row_diff
        self.col_diff = col_diff
        self.angle = angle
        self.id_line_to = id_line_to
